namespace: oow-test-cluster-cluster-dev

resources:
  - ../base
  - namespace.yaml

patches:
- path: cluster-claim.yaml 
  target:
    group: kubernetes.example.com
    version: v1alpha1
    kind: AksClusterClaim
    name: oow-test-cluster-cluster
- patch: |-
    - op: replace
      path: /metadata/name
      value: oow-test-cluster-cluster-dev  
  target:
    kind: AksClusterClaim     